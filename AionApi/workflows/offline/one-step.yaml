enabled: false
cron: 0/12 * * * * ?
variables:
  exe: '{{ env "DEV_HOME" }}\Aion\AionApi.TestApp\bin\Debug\net8.0\AionApi.TestApp.exe'

steps:
  - script: '{{ exe }}'
    args:
      - '--source one-step'
      - '--sleep 5'
      - '--exit-code 0'
    windowVisible: true