enabled: false
cron: 0/12 * * * * ?
variables:
  exe: '{{ env "DEV_HOME" }}\Aion\DummyApp\bin\Debug\net8.0\DummyApp.exe'

steps:
  - script: '{{ exe }}'
    args:
      - bar-0
      - 7000
      - 0
    name: foo
  - script: '{{ exe }}'
    args:
      - bar-1
      - 3000
      - 0
    name: bar
    dependsOn: '$previous'